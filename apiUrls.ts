import { Product } from "./pages/shop/shop.model";
const apiUrls = {
  login: "/login",
  register: "/register",
  product: "/products",
  categoryFilter: "/category/filter",
};

export default apiUrls;
